import StubPage from '@/components/ui/StubPage';
export default function SuperDashboard() {
  return <StubPage title="Platform Overview" route="/super" description="Total hospitals, appointments, active users across the entire platform." />;
}
