import StubPage from '@/components/ui/StubPage';
export default function DoctorQueuePage() {
  return <StubPage title="My Queue" route="/doctor/queue" description="Current patient, next patients, Call Next, Complete, add consultation note." />;
}
