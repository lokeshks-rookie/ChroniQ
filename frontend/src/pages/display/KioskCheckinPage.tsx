import StubPage from '@/components/ui/StubPage';
export default function KioskCheckinPage() {
  return <StubPage title="Kiosk Check-in" route="/checkin/:hospitalId" description="Patient enters phone or scans QR, gets a queue token — no reception needed." />;
}
