import StubPage from '@/components/ui/StubPage';
export default function ProfilePage() {
  return <StubPage title="Profile & Settings" route="/app/profile" description="Personal info, language preference, notification preferences, password." />;
}
