import StubPage from '@/components/ui/StubPage';
export default function CheckInPage() {
  return <StubPage title="Check-in Desk" route="/admin/checkin" description="Scan patient QR or search by phone or appointment ID, mark arrived." />;
}
