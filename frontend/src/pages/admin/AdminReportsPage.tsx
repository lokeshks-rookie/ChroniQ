import StubPage from '@/components/ui/StubPage';
export default function AdminReportsPage() {
  return <StubPage title="Reports & Analytics" route="/admin/reports" description="Footfall, wait times, peak hours, no-show rate, doctor utilization — export CSV or PDF." />;
}
