import StubPage from '@/components/ui/StubPage';
export default function AdminDashboard() {
  return <StubPage title="Admin Dashboard" route="/admin" description="Today's appointments, patients waiting, average wait, no-show rate, and live alerts." />;
}
