import StubPage from '@/components/ui/StubPage';
export default function DoctorsAdminPage() {
  return <StubPage title="Doctors" route="/admin/doctors" description="List, add, edit, deactivate doctors — assign departments and schedules." />;
}
