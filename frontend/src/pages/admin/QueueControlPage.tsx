import StubPage from '@/components/ui/StubPage';
export default function QueueControlPage() {
  return <StubPage title="Live Queue Control" route="/admin/queue" description="Per-doctor queue columns, Call Next, Skip, No-Show, Priority, Emergency insert." />;
}
